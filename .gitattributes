# Normalize to LF in the repo and on checkout. Without this, a Windows clone
# would check entrypoint.sh out with CRLF, which breaks its shebang inside the
# Linux container ("bad interpreter").
* text=auto eol=lf
*.sh text eol=lf
